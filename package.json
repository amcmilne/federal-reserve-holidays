{
  "name": "federal-reserve-holidays-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w @bank/federal-reserve-holidays && npm run build -w web",
    "test": "npm run test -w @bank/federal-reserve-holidays",
    "lint": "npm run lint -ws",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.6"
  }
}
